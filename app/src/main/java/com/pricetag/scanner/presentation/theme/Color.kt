package com.pricetag.scanner.presentation.theme

import androidx.compose.ui.graphics.Color

// ── Retail dark theme palette ─────────────────────────────────────────────────
val BgDark       = Color(0xFF0D1117)  // deep dark background
val BgSurface    = Color(0xFF161B22)  // card background
val BgInput      = Color(0xFF21262D)  // input / elevated surface
val PrimaryTeal  = Color(0xFF00BFA5)  // primary action — high visibility teal
val PrimaryGreen = Color(0xFF00C853)  // success / send button
val ErrorRed     = Color(0xFFFF5252)  // error / disconnect
val WarningAmber = Color(0xFFFFAB00)  // partial state / warning
val TextPrimary  = Color(0xFFF0F6FC)  // primary text — near white
val TextSecondary= Color(0xFF8B949E)  // secondary/hint text
val TextOnPrimary= Color(0xFF0D1117)  // text on teal/green buttons
val BorderColor  = Color(0xFF30363D)  // subtle borders
val SlotEmpty    = Color(0xFF21262D)  // unfilled 4PCS slot
val SlotFilled   = Color(0xFF0D3B2E)  // filled slot — deep green tint
val SlotFilledBorder = Color(0xFF00C853)
